experiment:
  name: "aggregation_srq2"
  seed: 42

scenario:
  file: "scenarios/footbot_aggregation_srq2.argos"

training:
  script: "training/aggregation.py"
  iterations: 5

output:
  dir: "results/"
