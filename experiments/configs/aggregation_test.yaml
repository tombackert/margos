experiment:
  name: "aggregation_test"
  seed: 42

scenario:
  file: "scenarios/footbot_aggregation.argos"

training:
  script: "training/aggregation.py"
  iterations: 5  # Short for testing

output:
  dir: "results/"
