# Backlog

- Show usage explainer as: "Usage: platform [run | report | export | import]"
- We need autocomplete on experiment filenames or faster way of choosing (currently typing date-strings in filenames)
- Need command platform show config/experiments/results